# Lead form API (all landing-page forms POST here)
# Local dev: use /api/leads (proxied to backend on port 3005 via vite.config.js)
# Production: use your deployed backend URL, e.g. https://api.yourdomain.com/api/leads
VITE_LEAD_API_URL=http://localhost:3005/api/leads
